Biosimulations_format_utils.import_resources package

Submodules

Biosimulations_format_utils.import_resources.biomodels module

Import models from BioModels into BioSimulations

Author:
   Jonathan Karr <

   karr@mssm.edu

   >

Date:
   2020-03-23

Copyright:
   2020, Center for Reproducible Biomedical Modeling

License:
   MIT

class Biosimulations_format_utils.import_resources.biomodels.BioModelsImporter(_max_models=inf, _cache_dir=None)

   Bases: "object"

   Import models from BioModels

   _max_models

      maximum number of models to download from BioModels

      Type:
         "int"

   _cache_dir

      directory to cache models from BioModels

      Type:
         "str"

   _requests_session

      requests cached session

      Type:
         "requests_cache.core.CachedSession"

   BIOMODELS_ENDPOINT = 'https://www.ebi.ac.uk/biomodels'

   BIOSIMULATIONS_ENDPOINT = 'https://api.biosimulations.dev'

   MAX_RETRIES = 5

   NUM_MODELS_PER_BATCH = 100

   PUBMED_ENDPOINT = 'https://eutils.ncbi.nlm.nih.gov/entrez/eutils/efetch.fcgi'

   get_model(id)

      Get a model

      Parameters:
         id

          ("str") -- model id

      Returns:
         information about model "list" of "Simulation": information
         about simulations

      Return type:
         "Model"

   get_model_batch(num_results=10, i_batch=0)

      Retrieve a batch of models

      Parameters:
         num_results (int, optional) -- number of results to retrieve

         i_batch (int, optional) -- index of batch to retrieve

      Returns:
         batch of models

      Return type:
         "dict"

   get_model_file(id, filename)

   get_model_files_metadata(id)

      Get metadata about the files of a model

      Parameters:
         id

          ("str") -- model id

      Returns:
         metadata about the files of a model

      Return type:
         "dict"

   get_model_metadata(id)

      Get metadata about a model

      Parameters:
         id

          ("str") -- model id

      Returns:
         metadata about the files of a model

      Return type:
         "dict"

   get_models()

      Get models from BioModels

      Returns:
         list of metadata about each model "list" of "Simulation":
         list of metadata about each simulation

      Return type:
         "list" of "Model"

   get_num_models()

      Get the number of models to import

      Returns:
         number of models to import

      Return type:
         "int"

   get_stats(models, sims)

      Calculate statistics about the imported models

      Parameters:
         models (list of Model) -- models

         sims (list of Simulation) -- simulations

      Returns:
         statistics about the imported models

      Return type:
         "dict"

   init_requests_cache()

   read_data()

      Read models and simulations from JSON files

      Returns:
         models "list" of "Simulation": simulations "dict": stats
         about the models

      Return type:
         "list" of "Model"

   run()

      Retrieve models from BioModels and submit to BioSimulations

      Returns:
         models "list" of "Simulation": simulations "dict": statistics
         about the models

      Return type:
         "list" of "Model"

   submit_models(models)

      Post models to BioSimulations

      Parameters:
         models

          ("list" of "Model") --

   viz_model(model)

      Generate a visualization of a model

      Parameters:
         model

          ("Model") -- model

      Returns:
         image

      Return type:
         "RemoteFile"

   write_data(models, sims, stats)

      Save models and simulations to JSON files

      Parameters:
         models (list of Model) -- models

         sims (list of Simulation) -- simulations

         stats (dict) -- statistics of the models

Module contents
